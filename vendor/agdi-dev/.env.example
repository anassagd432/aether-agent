# ─────────────────────────────────────────────
# Agdi Environment Variables
# Copy this file to .env and fill in your values
# ─────────────────────────────────────────────

# ==================== CLIENT-SIDE (VITE_*) ====================
# These are embedded in the browser bundle. Safe for public keys only.

# Supabase (required for auth — get from supabase.com/dashboard → Settings → API)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# App URL (must match your dev server)
VITE_APP_URL=http://localhost:5173

# ── AI Provider API Keys (BYOK — bring your own) ──
# Only needed if you want to use your own keys instead of cloud AI.
# Get Gemini key from: https://aistudio.google.com/apikey
VITE_GEMINI_API_KEY=

# Get OpenRouter key from: https://openrouter.ai/keys (access 100+ models including free ones)
VITE_OPENROUTER_API_KEY=

# Get OpenAI key from: https://platform.openai.com/api-keys
VITE_OPENAI_API_KEY=

# Get Anthropic key from: https://console.anthropic.com/
VITE_ANTHROPIC_API_KEY=

# Get DeepSeek key from: https://platform.deepseek.com/
VITE_DEEPSEEK_API_KEY=


# ==================== SERVER-SIDE (Vercel only) ====================
# ⚠️  WARNING: Do NOT put these in your .env file!
# ⚠️  Set these ONLY in Vercel Dashboard → Settings → Environment Variables.
# ⚠️  These have admin-level access and must never be in client-side code.

# SUPABASE_URL=            # Same as VITE_SUPABASE_URL
# SUPABASE_SERVICE_ROLE_KEY= # Supabase service role key (admin access)
# GEMINI_API_KEY=           # Google Gemini key (for cloud AI proxy)
# OPENROUTER_API_KEY=       # OpenRouter key (optional, for GPT-4o cloud)
# ALLOWED_ORIGIN=           # Production URL, e.g. https://agdi.dev

# ── Newsletter (optional) ──
# MAILCHIMP_API_KEY=
# MAILCHIMP_LIST_ID=

# ── Vercel Integration (for One-Click Deploy) ──
# Create a Vercel Integration to get these: https://vercel.com/integrations/console
# VERCEL_CLIENT_ID=
# VERCEL_CLIENT_SECRET=

# ── Web IDE Memory (Embeddings) ──
# Server-side only (Vercel env vars). Used by /api/embed.
# GEMINI_API_KEY=
